; react-simple-maps 3.0.0 declares a React 16/17/18 peer range and has no
; React 19 stable release, so a clean `npm ci` fails ERESOLVE even though the
; library works fine at runtime. Revisit when react-simple-maps 4.x ships.
legacy-peer-deps=true
